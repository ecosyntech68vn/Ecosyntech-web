EcoSynTech Local Core – Architecture (Mini)

- HTTP API (Express) + static dashboard assets
- src/core for business domains (iot, devices, rules, dashboards, etc.)
- SQLite/sql.js as test DB; production DB config optional
- AI bootstrap with optional large models
- Security components: RBAC, rate limiting, logging
